const $ = (s) => document.querySelector(s);
let items = [], editingId = null;
const esc = (s) => String(s ?? "").replace(/[&<>"']/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[c]));
async function api(path, opt) { const r = await fetch(path, { headers: { "content-type": "application/json" }, ...opt }); const d = await r.json().catch(() => ({})); if (!r.ok) throw new Error(d.error || r.statusText); return d; }
async function load() { items = await api("/api/items"); render(); }
async function saveHabit(data) { if (editingId) { const old = items.find((x) => x.id === editingId); await api("/api/items/" + editingId, { method: "PUT", body: JSON.stringify({ ...old, ...data, id: editingId }) }); editingId = null; } else { await api("/api/items", { method: "POST", body: JSON.stringify(data) }); } await load(); }
async function updateItem(item) { await api("/api/items/" + item.id, { method: "PUT", body: JSON.stringify(item) }); await load(); }
async function deleteItem(id) { if (confirm("Delete this item?")) { await api("/api/items/" + id, { method: "DELETE" }); if (editingId === id) editingId = null; await load(); } }
function shell(body) { document.body.innerHTML = `<main><header class="top"><div><h1>${esc(APP.name)}</h1><p class="muted">${esc(APP.desc)}</p></div></header><div id="app">${body}</div></main>`; }
function today() { return new Date().toISOString().slice(0, 10); }
function render() { const edit = items.find((x) => x.id === editingId); shell(`<section class="tool-layout"><aside class="panel"><h2>${edit ? "Edit Habit" : "New Habit"}</h2><form id="f"><input id="title" placeholder="Drink water" required value="${esc(edit?.title || "")}"><select id="frequency">${["daily", "weekdays", "weekly"].map((f) => `<option ${edit?.meta?.frequency === f ? "selected" : ""}>${f}</option>`).join("")}</select><textarea id="body" placeholder="Why this matters">${esc(edit?.body || "")}</textarea><div class="row"><button>${edit ? "Save habit" : "Add habit"}</button>${edit ? '<button type="button" class="ghost" id="cancel">Cancel</button>' : ""}</div></form></aside><section><div class="habit-grid">${items.map(habit).join("") || '<p class="muted">No habits yet.</p>'}</div></section></section>`); $("#f").onsubmit = (e) => { e.preventDefault(); saveHabit({ title: $("#title").value, body: $("#body").value, status: edit?.status || "active", meta: { frequency: $("#frequency").value, streak: edit?.meta?.streak || 0, last_checkin: edit?.meta?.last_checkin || "" } }); }; const c = $("#cancel"); if (c) c.onclick = () => { editingId = null; render(); }; }
function habit(x) { const checked = x.meta?.last_checkin === today(), streak = Number(x.meta?.streak || 0); return `<article class="item-card"><div class="row space"><h3>${esc(x.title)}</h3><span class="badge">${esc(x.meta?.frequency)}</span></div><p>${esc(x.body)}</p><strong>${streak} day streak</strong><div class="meter"><span style="width:${Math.min(100, streak * 10)}%"></span></div><p>${esc(x.meta?.last_checkin ? "Last: " + x.meta.last_checkin : "Not checked in")}</p><button ${checked ? "disabled" : ""} onclick="checkin(${x.id})">${checked ? "Done today" : "Check in"}</button><button class="ghost" onclick="editHabit(${x.id})">Edit</button><button class="danger" onclick="deleteItem(${x.id})">Delete</button></article>`; }
async function checkin(id) { const x = items.find((i) => i.id === id); x.meta.streak = Number(x.meta?.streak || 0) + 1; x.meta.last_checkin = today(); await updateItem(x); }
function editHabit(id) { editingId = id; render(); }
window.checkin = checkin; window.editHabit = editHabit; window.deleteItem = deleteItem; load();
